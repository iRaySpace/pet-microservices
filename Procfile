clinic: cd clinic/clinic && pipenv run uvicorn app:app --reload --port 8000
review: cd review/review && pipenv run uvicorn app:app --reload --port 8001
query: cd query/query && pipenv run uvicorn app:app --reload --port 8002
eventbus: cd eventbus/eventbus && pipenv run uvicorn app:app --reload --port 8010
web: cd front && npm start